services:
  api:
    image: histoseg-plugin:cpu
    build:
      context: .
      dockerfile: Dockerfile.cpu
    deploy:
      resources:
        reservations:
          devices: !reset []

  worker:
    image: histoseg-plugin:cpu
    deploy:
      resources:
        reservations:
          devices: !reset []