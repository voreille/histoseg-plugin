queue_db_url: sqlite:////app/data/histoseg_queue.db

allowed_roots:
  - /mnt/nas6
  - /mnt/nas7
  - /home/val/data
debug: true

results_root: /app/results
models_root: /app/models
logs_root: /app/logs

worker_poll_interval_seconds: 1.0
worker_heartbeat_seconds: 5.0
gpu_idle_unload_seconds: 300
stale_task_timeout_seconds: 60

default_model_id: default
preferred_device: cuda
use_amp: true

api:
  host: 0.0.0.0
  port: 8000
  reload: true
